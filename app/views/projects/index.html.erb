<h2>
        <span>Projects</span></h2>

		<ul class="list">
		<% @projects.each do |project| %>
		    <li><%= link_to project.name, edit_project_path(project)%>
		        <p>
		            <%= project.description%>
				</p>
		    </li>
		<% end %>